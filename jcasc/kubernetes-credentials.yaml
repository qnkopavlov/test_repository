credentials:
  system:
    domainCredentials:
      - credentials:
          - file:
              scope: GLOBAL
              id: "kubernetes-credentials"
              fileName: "/root/.kube/config"
              secretBytes: ${KUBE_CONFIG_BYTES}